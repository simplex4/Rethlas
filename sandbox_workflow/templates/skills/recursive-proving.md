# Explore remaining decomposition plans

<!-- Adapted from agents/generation/.agents/skills/recursive-proving/SKILL.md. -->

After all current plans have been screened using `skills/direct-proving.md` and none is complete, gather each plan's exact stuck points, counterexamples, failed paths, and relevant references.

Use native Codex delegation only if available and permitted by the current agent's inherited instructions. Never invoke a nested Codex process or a different model/account to obtain delegation. The runtime supports recursive native delegation within its configured limits: up to 10 agent threads and maximum depth 3. These are global runtime bounds, not a fresh allowance for each parent. A child may delegate bounded subproblems within its assigned plan when the remaining thread and depth budget permits. Honor stricter managed or runtime limits. If delegation is unavailable, prohibited, or a bound is reached, explore the same plans sequentially and log that limitation.

Delegate only independent bounded work while useful parent work continues. For each task specify the full target theorem, assigned plan, stuck points from all plans, one concrete output artifact, exclusive allowed files in a unique child directory, acceptance check, stop condition, and current delegation depth. Apply this same contract to any recursive child task, keeping it within the parent's assigned mathematical scope. Assign one owner per file. Before dispatch, populate the unique child directory with `research.py`, `research_contract.py`, `turn.json`, `statement.md`, required `references/`, needed skills, and an `AGENTS.md` combining the applicable research instructions with `subgoal.md`. Copy the current turn policy unchanged; have the child run helper commands from its own directory so its memory and downloads remain local. Configure or verify the exact same model and reasoning effort as the generator, the parent sandbox, and the parent turn search policy. If the available native delegation mechanism cannot preserve those settings, use sequential exploration and record why. The child must return findings for parent integration and must not write shared memory, submit a candidate, or publish acceptance.

Preserve continuity with each assigned plan while allowing evidence-driven local revisions. Gather each report and its complete local research records, source files, proof-essential computational artifacts with replay/version notes, and reference-read status; check claimed progress. Import useful records into parent memory preserving child/plan provenance, original channel, failed attempts, source identifiers, and local reference paths (copy references into parent-owned paths as necessary). Copy proof-essential computations into parent `artifacts/` and cited sources into parent `references/`, updating draft paths; the verifier receives those frozen directories, not private child or parent memory. Do not discard child memory after reading only its final summary. Persist the integrated findings to `proof_steps`, `failed_paths`, and `events`. Update branch states with `python3 research.py branch --id BRANCH --file record.json`. Record plan ids, child ids if any, shared stuck points, mode (native or sequential), outcomes, and remaining gaps. If a plan succeeds, assemble a full candidate for separate independent verification. If all fail, read `skills/identify-key-failures.md`, synthesize common obstructions, and propose new plans.
